{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "PostgreSQL connection. In production this is supplied via the ConnectionStrings__AppDb environment variable (see compose.yaml / .env). Local development overrides it in appsettings.Development.json.",
    "AppDb": ""
  }
}
